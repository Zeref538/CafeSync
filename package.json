{
  "name": "cafesync",
  "version": "1.0.0",
  "description": "Smart coffee shop management system with AI-powered insights",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "ai": "cd ai-services && python app.py",
    "build": "cd client && npm run build",
    "deploy": "cd client && firebase deploy",
    "build:deploy": "npm run build && npm run deploy",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install && cd ../ai-services && pip install -r requirements.txt"
  },
  "keywords": [
    "coffee",
    "management",
    "ai",
    "react",
    "nodejs",
    "python"
  ],
  "author": "CafeSync Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "firebase": "^12.4.0"
  }
}
